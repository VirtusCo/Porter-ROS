# Transport mock unit tests — run on native_sim
CONFIG_ZTEST=y
CONFIG_CPP=y
CONFIG_STD_CPP17=y
CONFIG_REQUIRES_FULL_LIBCPP=y

# Use mock transport backend (no real hardware needed)
CONFIG_PORTER_TRANSPORT=y
CONFIG_PORTER_TRANSPORT_MOCK=y
